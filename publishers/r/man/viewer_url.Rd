% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{viewer_url}
\alias{viewer_url}
\title{Get the viewer URL for a session.}
\usage{
viewer_url(session = NULL, copy = FALSE)
}
\arguments{
\item{session}{A \code{Session} from \code{\link[=session]{session()}}, or \code{NULL} (default)
for the ambient session.}

\item{copy}{If \code{TRUE}, also copy the URL to the system clipboard
via the \code{clipr} package (a \code{Suggests} dependency). If \code{clipr}
isn't installed or the clipboard isn't available, a warning is
issued and the URL is still returned.}
}
\value{
The viewer URL as a single character string. Errors if no
session is open.
}
\description{
Defaults to the ambient session — the same one \code{blick()},
\code{with_session()}, and \code{\link[=session]{session()}} all share. Pass a \code{Session}
explicitly to query a specific one.
}
