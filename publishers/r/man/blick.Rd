% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blick.R
\name{blick}
\alias{blick}
\title{Iterate with live progress streamed to viewers.}
\usage{
blick(items, fun, label = NULL, unit = "it")
}
\arguments{
\item{items}{A vector or list. Names are preserved on the result.}

\item{fun}{A function applied to each element of \code{items}.}

\item{label}{Optional human-readable label shown to viewers. If
\code{NULL} (default), the deparsed call is used (truncated).}

\item{unit}{Short label for "what one iteration represents" (e.g.
\code{"row"}, \code{"epoch"}, \code{"sim"}). Defaults to \code{"it"} to match the
Python SDK.}
}
\value{
A list of the same length as \code{items}, with the same names
if any (matches \code{lapply} semantics).
}
\description{
Drop-in replacement for \code{\link[=lapply]{lapply()}} that emits a progress update
after each element. The first call in an R process spawns the
bridge subprocess and prints the viewer URL; subsequent \code{blick()}
calls in the same process \strong{reuse the same session} — viewers
keep watching the same URL across runs.
}
\examples{
\dontrun{
result <- blick(1:100, function(i) {
  Sys.sleep(0.05)
  i * 2
})
}
}
