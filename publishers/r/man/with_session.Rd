% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scope.R
\name{with_session}
\alias{with_session}
\title{Scope a task around an expression and stream progress.}
\usage{
with_session(expr, label = NULL)
}
\arguments{
\item{expr}{Code to run with the task scope active. Evaluated in
the caller's environment; capture-by-reference, not deep-copied.}

\item{label}{Human-readable label shown to viewers. Defaults to
the auto-generated task ID.}
}
\value{
The value returned by \code{expr}, invisibly when \code{expr} returns
invisibly.
}
\description{
Evaluates \code{expr} in the caller's environment with an active task
scope. Inside \code{expr}, calls to \code{tick(value = ..., ...)} (without
an explicit session/task_id) automatically target this scope.
}
\details{
Reuses the per-process ambient session — no new bridge subprocess
is spawned. The first \code{with_session()} (or \code{\link[=session]{session()}} / \code{blick()})
call in an R process spawns the bridge; subsequent calls share it.

Nested \code{with_session()} calls work: the inner scope replaces the
outer task on the wire (the bridge implicitly ends the previous
task on each new START), and on exit the outer task is restarted
so subsequent ticks update it again.
}
