% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{viewers}
\alias{viewers}
\title{Current viewer roster.}
\usage{
viewers(session = NULL)
}
\arguments{
\item{session}{A \code{Session} from \code{\link[=session]{session()}}, or \code{NULL} (default)
for the ambient session.}
}
\value{
Character vector of viewer names (length 0 if nobody's connected).
}
\description{
Returns the names of viewers currently connected to the session.
The list is updated as \code{viewer_joined} / \code{viewer_left} events
arrive from the bridge; calling this function drains pending events
first so the result reflects the latest state.
}
