% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manual.R
\name{session}
\alias{session}
\title{Open or reuse the ambient Fernsicht session.}
\usage{
session(
  label = NULL,
  server_url = NULL,
  join_secret = NULL,
  max_viewers = NULL,
  bridge_path = NULL
)
}
\arguments{
\item{label}{Optional human-readable annotation for this session,
surfaced in \code{print()} and \code{\link[=diagnostics]{diagnostics()}}. Not transmitted to the
bridge or viewers.}

\item{server_url, join_secret, max_viewers, bridge_path}{Configuration
overrides. See plan §4.5 for the resolution order
(arg > option > env > default).}
}
\value{
A \code{Session} R6 object. Typically you'll just hand it to
\code{\link[=start_task]{start_task()}} / \code{\link[=tick]{tick()}} / \code{\link[=end_task]{end_task()}} without inspecting it.
}
\description{
Returns the per-process ambient session, lazy-spawning the bridge
subprocess on first use. Subsequent calls in the same R process
return the same Session object — the viewer URL stays stable.
}
