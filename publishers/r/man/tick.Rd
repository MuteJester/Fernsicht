% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manual.R
\name{tick}
\alias{tick}
\title{Emit a progress update for an active task.}
\usage{
tick(...)
}
\arguments{
\item{...}{See above. Named args (\code{value}, \code{n}, \code{total}, \code{rate},
\code{elapsed}, \code{eta}, \code{unit}) match the bridge's wire fields.}
}
\value{
The \code{session}, invisibly.
}
\description{
Two call forms:
}
\details{
\describe{
\item{Layer 3 (explicit)}{\code{tick(session, task_id, value, ...)} —
supply the session and task_id directly.}
\item{Layer 2 (implicit)}{\code{tick(value, ...)} — uses the ambient
session and the task on top of the \code{with_session()} stack
(added in a later phase). Errors clearly if no scope is active.}
}
}
